{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "python scripts/pipeline.py --max 40 && python scripts/brief.py daily",
    "cronSchedule": "0 */6 * * *",
    "restartPolicyType": "NEVER"
  }
}
