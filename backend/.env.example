# Copy to .env for local dev (gitignored). In prod, Railway injects DATABASE_URL.
# Locally use the Railway PUBLIC proxy URL (DATABASE_PUBLIC_URL from `railway variables`).
DATABASE_URL=postgresql://user:pass@host:port/db

VOYAGE_API_KEY=
GROQ_API_KEY=
ANTHROPIC_API_KEY=

SYNTHESIS_PROVIDER=groq
